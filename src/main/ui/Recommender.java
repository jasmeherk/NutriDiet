package ui;

public class Recommender {

    private static double sigmoid(double x) {
        return  (1/( 1 + Math.pow(Math.E,(x*-1))));
    }
    public String calorieMessage(int calories) { // based on how close I am to weight
        if
    }
    public String hydrationMessage(int water) { // based on how close I am to hydration goal

    }
    public void getMessage(){
        if ()
    };

}

